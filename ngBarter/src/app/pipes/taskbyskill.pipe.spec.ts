import { TaskbyskillPipe } from './taskbyskill.pipe';

describe('TaskbyskillPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskbyskillPipe();
    expect(pipe).toBeTruthy();
  });
});
